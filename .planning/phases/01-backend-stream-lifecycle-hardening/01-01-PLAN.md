---
phase: 01-backend-stream-lifecycle-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/main/server/router.ts
  - electron/main/server/claude-code-processor.ts
autonomous: true

must_haves:
  truths:
    - "User sends message to 302.AI Code Agent and loading spinner disappears within 100ms of last token"
    - "User cancels 302.AI Code Agent stream and loading indicator clears immediately"
    - "User encounters network error during 302.AI Code Agent stream and receives error message with loading state clearing"
  artifacts:
    - path: "electron/main/server/router.ts"
      provides: "302.AI Code Agent endpoint with guaranteed stream closure"
      min_lines: 1650
      contains: "finally"
    - path: "electron/main/server/claude-code-processor.ts"
      provides: "SSE processor with guaranteed finish event emission"
      min_lines: 950
      contains: "controller.close"
  key_links:
    - from: "electron/main/server/router.ts"
      to: "controller.close()"
      via: "finally block guaranteed closure"
      pattern: "finally\\s*\\{[^}]*controller\\.close"
    - from: "electron/main/server/claude-code-processor.ts"
      to: "finish event"
      via: "flushBuffer sends pending finish"
      pattern: "pendingFinishEvent.*finish"
---

<objective>
Fix critical stream lifecycle gaps in 302.AI Code Agent endpoint to guarantee controller.close() is called in all code paths.

Purpose: The 302.AI Code Agent endpoint has missing finally blocks and multiple early returns that can leave streams unclosed, causing loading spinners to persist indefinitely. This plan adds guaranteed closure semantics.

Output: 302.AI Code Agent endpoint with bulletproof stream lifecycle management—streams always close properly even on errors, aborts, or network failures.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-stream-lifecycle-hardening/01-RESEARCH.md
@electron/main/server/router.ts
@electron/main/server/claude-code-processor.ts
@electron/main/server/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Add finally block with safeClose pattern to 302.AI Code Agent endpoint</name>
  <files>electron/main/server/router.ts</files>
  <action>
Wrap the 302.AI Code Agent endpoint's ReadableStream start() handler (lines 1560-1637) with a try-catch-finally block that guarantees controller.close() is called exactly once.

Implementation pattern (from RESEARCH.md):
1. Add `let streamClosed = false;` flag at start of start() handler
2. Create `safeClose()` helper function that:
   - Checks if streamClosed is false
   - Calls controller.close() wrapped in try-catch (ignore already-closed errors)
   - Sets streamClosed = true
3. Wrap ALL streaming logic in try-catch-finally
4. In catch block: call sendStreamError() and set streamClosed = true (because sendStreamError calls controller.close)
5. In finally block: call safeClose() to ensure closure if not already closed

**Critical:** Do NOT call controller.close() after sendStreamError()—sendStreamError already closes it. The safeClose() pattern prevents double-close errors.

**Preserve existing behavior:**
- Keep immediate start event emission (line 1563-1564)
- Keep attachment upload logic (lines 1568-1576)
- Keep reader loop and error handling (lines 1608-1631)
- Keep all existing console.log statements for debugging

**Why this matters:** Multiple early returns (lines 1574, 1596, 1605) can skip closure. The reader error path (line 1630) throws without closing. The finally block guarantees closure in ALL paths.
  </action>
  <verify>
1. Search for "finally" in router.ts line 1560-1650: `grep -n "finally" electron/main/server/router.ts | grep -A5 -B5 "156[0-9]"`
2. Verify safeClose pattern exists: `grep -n "safeClose" electron/main/server/router.ts | head -5`
3. Confirm streamClosed flag guards sendStreamError: `grep -n "streamClosed = true" electron/main/server/router.ts | head -3`
4. Check that controller.close() is NOT called after sendStreamError: `grep -A3 "sendStreamError" electron/main/server/router.ts | grep -v "controller.close()"`
  </verify>
  <done>
- 302.AI Code Agent endpoint has try-catch-finally block wrapping all streaming logic
- safeClose() helper function exists and is called in finally block
- streamClosed flag prevents double-close after sendStreamError
- All console.log debug statements are preserved
- No regressions: existing functionality unchanged, only lifecycle hardening added
  </done>
</task>

<task type="auto">
  <name>Add [DONE] marker to ClaudeCodeProcessor flushBuffer</name>
  <files>electron/main/server/claude-code-processor.ts</files>
  <action>
Ensure ClaudeCodeProcessor emits explicit [DONE] marker when stream completes, per AI SDK SSE protocol requirements.

Current state (lines 837-859):
- flushBuffer() sends pending finish event if present
- Does NOT send [DONE] marker after finish event

Fix:
1. In flushBuffer() method (line 837), after appending pendingFinishEvent (line 854), append `data: [DONE]\n\n` marker
2. The [DONE] marker must come AFTER the finish event, per AI SDK protocol
3. Only send [DONE] if we sent a finish event (when pendingFinishEvent was not null)

**Pattern:**
```typescript
// If we have a pending finish event, append it
if (this.pendingFinishEvent) {
  result += this.pendingFinishEvent + "\n\n";
  this.pendingFinishEvent = null;
  // CRITICAL: Send [DONE] marker after finish event
  result += "data: [DONE]\n\n";
}
```

**Why this matters:** The AI SDK's SSE parser waits for [DONE] marker before triggering onFinish callback. Without it, the frontend never knows the stream is complete, even though finish event was sent.

**Preserve existing behavior:**
- Keep all event transformation logic unchanged
- Keep pendingFinishEvent storage mechanism
- Keep message-metadata event handling
- Keep all console.debug statements
  </action>
  <verify>
1. Search for [DONE] in flushBuffer: `grep -n "\[DONE\]" electron/main/server/claude-code-processor.ts | grep -A2 -B2 "flushBuffer"`
2. Verify [DONE] comes after finish event: `grep -A5 "pendingFinishEvent" electron/main/server/claude-code-processor.ts | grep -B1 "\[DONE\]"`
3. Confirm [DONE] is only sent when finish event exists: Check that [DONE] is inside the `if (this.pendingFinishEvent)` block
  </verify>
  <done>
- flushBuffer() method sends `data: [DONE]\n\n` marker after finish event
- [DONE] marker is only sent when pendingFinishEvent was not null
- [DONE] appears in the final output string after the finish event
- No changes to event transformation logic or state tracking
- Existing OpenAI streaming mode already has [DONE] (lines 219, 339) - unchanged
  </done>
</task>

<task type="auto">
  <name>Add stream completion debug logging to 302.AI Code Agent endpoint</name>
  <files>electron/main/server/router.ts</files>
  <action>
Add explicit console.log statements to verify stream closure in the 302.AI Code Agent endpoint for debugging stream lifecycle issues.

Add logging in these locations (within the modified stream start handler):
1. In safeClose() helper: `console.log("[302ai-code-agent] Stream closed via safeClose");`
2. In finally block before calling safeClose(): `console.log("[302ai-code-agent] Finally block reached, ensuring stream closure");`
3. In catch block after sendStreamError: `console.log("[302ai-code-agent] Error sent, stream closed via sendStreamError");`
4. In reader loop done path (line 1612): `console.log("[302ai-code-agent] Reader done, closing stream");`

**Why this matters:** These logs allow developers to verify in terminal output that streams are properly closing in all paths (success, error, abort). Per ROADMAP.md Phase 1 Success Criterion 3: "Developer inspects backend logs and sees explicit 'stream closed' events."

**Keep existing logs:** Preserve all existing console.log/console.error statements (lines 1564, 1581-1591, 1599, 1619, 1626, 1633).
  </action>
  <verify>
1. Search for new logging: `grep -n "stream closed" electron/main/server/router.ts | grep "302ai-code-agent"`
2. Count log statements added: Should find at least 4 new "stream closed" messages
3. Verify finally block logging: `grep -n "Finally block reached" electron/main/server/router.ts`
4. Verify safeClose logging: `grep -n "safeClose" electron/main/server/router.ts | grep "console.log"`
  </verify>
  <done>
- Four new console.log statements added for stream lifecycle tracking
- Logs clearly identify which code path closed the stream (safeClose, sendStreamError, reader done)
- All existing debug logs are preserved
- Log messages follow existing [302ai-code-agent] prefix convention
  </done>
</task>

</tasks>

<verification>
**Automated checks:**
1. TypeScript compilation passes: `pnpm run check`
2. ESLint passes: `pnpm run lint`
3. No runtime errors when starting dev server: `pnpm run dev` (wait 10 seconds, check console for errors)

**Manual stream lifecycle verification:**
1. Start app: `pnpm run dev`
2. Send message to 302.AI Code Agent endpoint
3. Check terminal logs for "stream closed" messages
4. Cancel a message mid-stream (if possible) and verify "stream closed" appears
5. Trigger an error (invalid API key) and verify error message shows + "stream closed via sendStreamError"

**Expected log output (success path):**
```
[302ai-code-agent] Sent immediate start event
[302ai-code-agent] Response status: 200 OK
[302ai-code-agent] Got response, streaming...
[302ai-code-agent] Reader done, closing stream
[302ai-code-agent] Finally block reached, ensuring stream closure
[302ai-code-agent] Stream closed via safeClose
```

**Expected log output (error path):**
```
[302ai-code-agent] Sent immediate start event
[302ai-code-agent] API error: 401 Unauthorized
[302ai-code-agent] Stream error: ...
[302ai-code-agent] Error sent, stream closed via sendStreamError
[302ai-code-agent] Finally block reached, ensuring stream closure
(safeClose detects already closed, no double-close)
```
</verification>

<success_criteria>
**Functional:**
- User sends message to 302.AI Code Agent and loading spinner disappears immediately after last token (within 100ms)
- User cancels stream mid-response and loading indicator clears instantly
- Network error mid-stream shows error message and clears loading state

**Technical:**
- 302.AI Code Agent endpoint has try-catch-finally block with safeClose pattern
- ClaudeCodeProcessor flushBuffer emits [DONE] marker after finish event
- Terminal logs show "stream closed" for every request (success, error, cancel)
- No double-close errors in logs
- TypeScript and ESLint pass with no new errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-stream-lifecycle-hardening/01-01-SUMMARY.md`
</output>
